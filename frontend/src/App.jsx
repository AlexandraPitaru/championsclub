export default function App() {
  return (
    <main className="app-shell">
      <section className="app-card">
        <p className="app-label">Frontend scaffold</p>
        <h1 className="app-title">ChampionsClub frontend placeholder</h1>
        <p className="app-copy">
          This container only provides the minimal React and Vite skeleton needed for Docker,
          local startup, and later handoff to the frontend team.
        </p>
      </section>
    </main>
  );
}